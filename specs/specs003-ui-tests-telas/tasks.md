# Tasks: specs003-ui-tests-telas

> Deriva de [plan.md](./plan.md). Convenções: `[P]` = paralelizável.
> Padrões do repo: testes de componente em jsdom seguindo
> `app/__tests__/page.test.tsx` (estado `vi.hoisted` + factory de mock de
> hooks + helpers `ok()/loading()/error()` + `afterEach(cleanup)`);
> strings formatadas sempre via `brl()`/`dateBR()`/`dateTimeBR()` de
> `@/src/lib/format` (NBSP do `Intl` pt-BR); proibidos snapshots e asserts
> de className decorativo (constituição art. 5). Todo o código testado JÁ
> EXISTE → fase RED usa a rota documentada: testes passam de imediato =
> comportamento já implementado → VALIDATE (falha = bug real → issue, ou
> teste errado → corrigir o teste).

## Onda 1 — Config (serial, pré-requisito de toda a onda 2)

| ID | Descrição | AC | Dependências | Globs permitidos | Status |
|---|---|---|---|---|---|
| T-001 | **Config** — 2 edits acoplados em `vitest.config.ts`: (1) projeto `ui` (linha 17): `include` → `["app/**/*.test.tsx", "src/components/**/*.test.tsx"]`; (2) projeto default (linha 11): `include` → `["src/lib/**/*.test.*"]` (o include antigo `src/**/*.test.*` casaria os novos `.test.tsx` de `src/components` e os duplicaria no ambiente node — o vitest 4 resolve `include` por projeto independentemente). Nada mais muda na config. Smoke obrigatório: `npm test` mantém os 64 testes verdes nos mesmos 11 arquivos, nenhum duplicado. | AC-010 | — | `vitest.config.ts` | concluída |

## Onda 2 — 8 arquivos de teste (TODA em paralelo, globs disjuntos)

| ID | Descrição | AC | Dependências | Globs permitidos | Status |
|---|---|---|---|---|---|
| T-002 [P] | **test-author** — criar `src/components/__tests__/ui.test.tsx` (projeto `ui`, sem mocks — DOM puro): `Card` (title/action/children; sem title/action → sem heading), `Stat` (label/value/hint; `tone` → `text-pos`/`text-neg` no `<p>` do valor), `connectionStatusBadge` (`UPDATED`→green/"Atualizada", `LOGIN_ERROR`→red/"Erro de login", desconhecido→yellow/label cru), `billStatusBadge` (PAID/PAST_DUE_UNPAID/PAST_DUE_UNCONFIRMED/OPEN/`undefined`→"—"), `ErrorState` (retry só com `onRetry`), `EmptyState` (âncora `target="_blank"` só com `href`+`linkLabel`), `AmountByKind` (cor por kind + `brl(1234.5)`), `KindBadge` (`KIND_LABEL` de `@/src/lib/semantics`), `PageHeader`, e `barFieldFromClick` (`ui.tsx:175-188`: `{key}`, `{payload:{key}}`, `{activePayload:[…]}` → string; não-string/`null`/`undefined` → `null`). | AC-001 | T-001 | `src/components/__tests__/ui.test.tsx` | concluída |
| T-003 [P] | **test-author** — criar `src/components/__tests__/sidebar.test.tsx`: mocks `next/navigation` (`usePathname` hoisted) e `@/src/lib/hooks` (`useSyncMeta` hoisted); `process.env.NEXT_PUBLIC_APP_VERSION = "0.0.0-test"` antes do render. Casos: 6 rotas com hrefs exatos e labels PT-BR, cada uma 2× (nav mobile + aside desktop); estado ativo por pathname (`/` ativo só em `/` exato — `/cartoes` não ativa `/`); `SyncChip` com `vi.useFakeTimers()` + `vi.setSystemTime(new Date("2026-08-05T12:00:00Z"))`: syncing→"Sincronizando…", erro→"Falha ao consultar sync", `syncedAt:null`→"Primeira sincronização…", `timeAgo` com 20 s→"agora" (nunca 30 s — fronteira de `Math.round`), 5 min→"há 5 min", 2 h→"há 2 h", `lastError` no atributo `title`; rodapé "v0.0.0-test". | AC-002 | T-001 | `src/components/__tests__/sidebar.test.tsx` | concluída |
| T-004 [P] | **test-author** — criar `src/components/__tests__/chat-dock.test.tsx`: mocks obrigatórios de `ai` (`DefaultChatTransport` classe stub) e `@ai-sdk/react` (`useChat` devolve estado hoisted `messages/status/error` + spies `sendMessage/stop/setMessages`, captura `opts`); `next/navigation` (`useRouter`). Casos: abrir/fechar via aria-labels ("Abrir assistente"/"Fechar assistente", `chat-dock.tsx:178/203`); 4 sugestões (`chat-dock.tsx:11-16`) e clique → `sendMessage({text})`; formulário (vazio → "Enviar" disabled; submit → envio + input limpo); busy (`status:"submitted"` → "Analisando…", "Parar processamento" → `stop`, submit/sugestão no-op); `error` visível; render de mensagens (tool-part `state:"output-available"` → chip "✓ …", sem estado → sem ✓; markdown renderiza `<strong>`); hidratação de `finmonitor.chat.v1` (`chat-dock.tsx:18`) — só parts `text` sobrevivem via `persistableMessages` (23-27), mensagem sem part text descartada, JSON corrompido/não-array → `undefined`; persistência no re-render (só parts text, teto de 40); "Limpar conversa" (habilitado só com mensagens; `setMessages([])` + key removida); tamanho default 420×560 e hidratado de `finmonitor.chat.size.v1` (`{"w":500,"h":400}`). **Contingência:** se os pointer events de redimensionamento por arraste não funcionarem em jsdom, dropar esses asserts (manter default/hidratado) e anotar no relatório. | AC-003 | T-001 | `src/components/__tests__/chat-dock.test.tsx` | concluída |
| T-005 [P] | **test-author** — criar `app/__tests__/conexoes.test.tsx`: hooks `useConnections`/`useSync`/`useDisconnect` mockados (mutações como `{mutate, isPending, isError, error}`). Casos: erro → `ErrorState` + retry chamando `refetch`; loading → "Carregando…" + header "Conexões"; vazio → EmptyState "Nenhuma conexão" + link "Adicionar banco" com `add_connection_url` (também no header quando a URL existe); card feliz (`connector_name`, badge via `connectionStatusBadge` REAL — NÃO mockar o componente, `item_id`, "conectada em {dateTimeBR}"); "Reconectar" presente só com `reconnect_url` (`target="_blank"`); sincronizar (clique → `sync.mutate`; `isPending` → "Sincronizando…" disabled; erro visível); desconexão em dois passos (1º clique → "Confirmar desconexão?", 2º → `disconnect.mutate({item:"it1", confirm:true})`; `isPending` → "Removendo…" disabled); `status:"LOGIN_ERROR"` → badge "Erro de login". | AC-004 | T-001 | `app/__tests__/conexoes.test.tsx` | concluída |
| T-006 [P] | **test-author** — criar `app/__tests__/emprestimos.test.tsx`: hooks `useConnections`/`useLoans` mockados; `LoanDetail` usa `useQuery` + `fetch` reais → render dentro de `QueryClientProvider` com `new QueryClient({defaultOptions:{queries:{retry:false}}})` e `globalThis.fetch = vi.fn()` (única exceção à política de mocks). Casos: `connections.isError`/`loans.isError` → ErrorState; loading → skeleton + header "Empréstimos"; `results` vazio → "Nenhum empréstimo contratado"; card (badge com `loan.type`, `brl(contractAmount)`, "Contrato nº {contractNumber} · vencimento {dateBR(dueDate)}"); toggle "Detalhes" → fetch POST `/api/bank/loans` body `{"loan_ids":[id]}` → `waitFor` e assertar contratado em, CET 2.5%, periodicidade MENSAL, linha de taxa "JUROS (SIMPLES)" "1.9% a.a.", "Total de parcelas" 24/"Parcelas restantes" 12; balloon (sem parcelas + `balloonPayments`) → "Pagamento único (balloon) em {dateBR} · {brl(5000)}"; detalhe vazio (`results:[]`) → "Sem detalhes para este contrato."; fetch `ok:false` com `{error:"boom"}` → "boom"; clique novamente colapsa. | AC-005 | T-001 | `app/__tests__/emprestimos.test.tsx` | concluída |
| T-007 [P] | **test-author** — criar `app/__tests__/cartoes.test.tsx`: hooks `useAccountsBundle`/`useBills`/`useBillsComparison` mockados. Casos: bundle error → ErrorState; loading → skeleton + header "Cartões"; sem conta `type:"CREDIT"` → "Nenhum cartão de crédito conectado"; painel do cartão (conta CREDIT com `creditData` → nome, "Mastercard · Black", "Fatura atual" `brl(balance)`, "Limite usado {brl} de {brl}" com "60%", "Limite disponível", "Vencimento" `dateBR`, "Pagamento mínimo" `brl`); disputa (`bills.data.disputed[b1]` → fatura atual reduzida + "Exclui {brl} pagos em {dateBR} (banco ainda não baixou)."); tabela de faturas (linhas com `dateBR(dueDate)`, `brl(totalAmount)`, `brl(minimumPaymentAmount)`, badges "Paga"/"Em aberto" via `billStatusBadge` real; fatura em `disputed` ganha selo "Pagamento registrado em … — aguardando baixa"); filtros (status "Paga" → só linha PAID; "A vencer (futuras)" sem match → "Nenhuma fatura com esses filtros"; vazio → "Nenhuma fatura"); `BillsComparisonCard` (loading → Skeleton, erro → ErrorState, `current`/`previous` null → "Menos de duas faturas pagas", feliz → `brl(1200)` em destaque + literal "+20,0%" — `toFixed(1).replace(".", ",")`, NÃO `brl`); bills error → ErrorState dentro do card "Faturas". | AC-006 | T-001 | `app/__tests__/cartoes.test.tsx` | concluída |
| T-008 [P] | **test-author** — criar `app/__tests__/investimentos.test.tsx`: stub `globalThis.ResizeObserver` (PieChart); hooks `useInvestments`/`useBenchmarks`/`useInvestmentMovements` mockados. Casos: erro → ErrorState; loading → 4 skeletons + header "Investimentos"; `results:[]` → "Nenhum investimento encontrado"; filtro que esvazia (B encerrada `TOTAL_WITHDRAWAL` some no default e a opção "ETF" só surge no select de tipos após marcar o checkbox "Exibir posições encerradas"; selecionar "ETF" e desmarcar o checkbox → "Nenhuma posição com esse filtro" + hint "Ajuste o tipo ou exiba encerradas."; marcar checkbox de encerradas → B aparece com badge "Encerrada"); stats agregados ("Total investido" `brl(3000)`, "Valor atual" `brl(3500)`, "Impostos provisionados" `brl(50)`, "Rentabilidade" `brl(450)` com `text-pos` e hint "15,00% líquido de impostos"); `rateLabel` literal "105.5% % do CDI" (rate cru via template string, sem vírgula/toFixed; sem rate → "—"); "a.a. realizado" (sem datas → "—"; ~2 anos via `new Date(Date.now() - 2*365.25*864e5)` → parse do texto da célula e assert em FAIXA 4–6, nunca valor exato); agenda (sem `dueDate` → "Sem vencimentos informados"; item → nome, "vence {dateBR}", `brl(amountWithdrawal)`); benchmarks (loading → Skeleton; `cdiAnnualPct:10.5/ipcaAnnualPct:4.2` → "10,50%"/"4,20%" + "Ganho real ≈ …"; `cdiAnnualPct:null` → "—"); distribuição por tipo (legenda agrega FIXED_INCOME + ETF); expansão de posição (loading → "Carregando movimentações…"; erro → mensagem; vazio → "Sem movimentações registradas."; rows `BUY`/`SELL` → "Aplicação"/"Resgate" com `dateBR`; colapso no 2º clique). | AC-007 | T-001 | `app/__tests__/investimentos.test.tsx` | concluída |
| T-009 [P] | **test-author** — criar `app/__tests__/transacoes.test.tsx` (a mais complexa): stub `globalThis.ResizeObserver` (BarChart); mocks hoisted capturando o payload de `useTransactions` e `useSearchParams` (`next/navigation`). Casos: estados (bundle error, tx loading → 6 skeletons, tx error, `results:[]` → "Nenhuma transação no período"); happy path (subtitle "{total} transação(ões) no período com os filtros atuais"; linha com `dateBR`, descrição, `AmountByKind` `brl` com `text-neg`, KindBadge "Gasto", badge "Confirmada"/"Pendente"); summary ("Entradas (filtro atual)"/"Saídas (filtro atual)"/"Resultado (filtro atual)" via `brl`); filtros refletidos no payload capturado (conta → `account_id`; período "90" → `from` = `daysAgoISO(90)` com prefixo ISO de 10 chars sem `to`; "Personalizado" → inputs de data, "De" → `from`; "Só gastos" → `kind:"spend"`; busca → `search_queries:["mercado"]`; categoria → `category`); card "Por categoria — gastos (filtros atuais) — clique na barra para filtrar" presente (clique no Bar do recharts NÃO exercido em jsdom — `barFieldFromClick` coberto na T-002); paginação ("Página 1 de 3", "Anterior" disabled, "Próxima" → `page === 2`, última página → "Próxima" disabled); chip de recorrência (`?desc=a&desc=b` → chip visível + `desc_norm:["a","b"]`; clique no chip → some e vira `undefined`); chip multi-janela (2 pares `fromIso/toIso` → `windows` e `from/to` undefined; clique → `windows` undefined e `from` restaurado); drill-down do dashboard (`range=custom&fromIso&toIso&kind&category` → selects refletidos e `captured.txParams.from === "2026-07-01T03:00:00.000Z"` — TIMESTAMP COMPLETO preservado); export CSV (`globalThis.fetch` + stubs `URL.createObjectURL/revokeObjectURL` + spy em `HTMLAnchorElement.prototype.click`: POST com `page_size:5000`; Blob começa com `\uFEFF` + header `data;descricao;categoria;valor;tipo;status`; escape `""Pão""`; valor `-12,5`; "Gasto"; `download` = `transacoes-todas-{baseFrom.slice(0,10)}.csv`; sem dados → botão disabled; `res.ok:false` 500 → "Erro 500 ao exportar"; reject `new Error("offline")` → "offline"); recategorização (select da linha → `categorize.mutate([{transaction_id:"t1", category_id}])`; `isPending` → "salvando…"; `isError` → mensagem). | AC-008 | T-001 | `app/__tests__/transacoes.test.tsx` | concluída |

> **Rota RED → VALIDATE:** todo o código testado já existe (diferente da
> specs002, nenhuma função é extraída ou criada). Os testes devem passar de
> imediato; a falha indica bug real (→ issue via `ship.mjs new --bug`, sem
> correção nesta entrega) ou teste errado (corrigir o teste — nunca
> enfraquecer com `test.skip`/asserts vazios, constituição art. 5).

## Pós-ondas — pipeline do orquestrador

| Fase | Ação |
|---|---|
| REFACTOR | `refactorer` invocado e registrado `refactor.status: SKIPPED` com `reason_if_skipped: "nenhum código de produção muda — apenas arquivos de teste novos e o include do vitest.config.ts"` (rota prevista na skill; nunca silencioso) |
| REVIEW | `peer-reviewer` independente: (a) nenhum arquivo de produção alterado — diff só em `vitest.config.ts` + testes novos; (b) testes não enfraquecidos (sem `test.skip`, sem asserts vazios); (c) constituição art. 5 — cada teste defende contrato observável e falha diante de bug plausível |
| VALIDATE | `validator`: `npm run typecheck`, `npm test` (64 pré-existentes + novos, sem duplicados — AC-009/AC-010), `npm run build` |

## Fora de Escopo

- Screenshots / snapshot visual / testes de pixel.
- Qualquer mudança em código de produção (bug exposto por teste → issue nova
  ao final, não correção aqui).
- Extração de lógica para `src/lib` (nenhuma função criada ou movida —
  diferença deliberada vs specs002).
- Alteração em `.github/workflows/ci.yml` e `constitution.md`. (O
  `vitest.config.ts` recebe as 2 edições de include documentadas — T-001,
  ver [plan.md](./plan.md).)
